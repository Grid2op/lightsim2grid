list(APPEND SOURCES "${CMAKE_CURRENT_SOURCE_DIR}/SparseLUSolver.cpp")

if(KLU_SOLVER_FOUND)
    list(APPEND SOURCES "${CMAKE_CURRENT_SOURCE_DIR}/KLUSolver.cpp")
endif()

if(LIB_CKTSO)
    list(APPEND SOURCES "${CMAKE_CURRENT_SOURCE_DIR}/CKTSOSolver.cpp")
endif()

if(LIB_NICSLU)
    list(APPEND SOURCES "${CMAKE_CURRENT_SOURCE_DIR}/NICSLUSolver.cpp")
endif()

set(SOURCES ${SOURCES} PARENT_SCOPE)
